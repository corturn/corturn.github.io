# Only needed if you want to preview the site locally before pushing.
# GitHub Pages builds the site without this file.
#
# The `github-pages` gem pins the exact versions GitHub uses, so a local
# preview matches production.

source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins

# Required on Ruby 3.0+
gem "webrick", "~> 1.8"
